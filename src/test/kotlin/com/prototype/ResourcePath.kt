package com.prototype

object ResourcePath {
    object JSON {
        private const val BASE_DIR = "/json/"
        const val findByIdで1を指定した場合の期待値 = BASE_DIR + "response/user/findByIdで1を指定した場合の期待値.json"
    }
}